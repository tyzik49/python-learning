		# 1. Реализовать функцию, принимающую два числа (позиционные аргументы) и выполняющую их деление.
		# Числа запрашивать у пользователя, предусмотреть обработку ситуации деления на ноль.


def delimetr(val1, val2):
    try:
        return val1 / val2
    except ZeroDivisionError:
        return "Делить на ноль нельзя!"

while True:
    user = input("Хотите продолжить y/n?")
    if user == "y":
        valint1 = int(input("Введите первое число:"))
        valint2 = int(input("Введите второе число:"))

    else:
        print("Хорошего Вам дня!")
        break

    print(f"Ваш результат: {delimetr(valint1, valint2)}")








		# 2. Реализовать функцию, принимающую несколько параметров, описывающих данные пользователя:
		# имя, фамилия, год рождения, город проживания, email, телефон.
		# Функция должна принимать параметры как именованные аргументы.
		# Реализовать вывод данных о пользователе одной строкой.

	#  Вариант 1

# users = [input("Введите имя:"),
#          input("Введите Фамилию:"),
#          input("Введите год рождения:"),
#          input("Введите город проживания:"),
#          input("Введите email:"),
#          input("Введите телефон:")
#          ]
# def user(*userdata):
#     print(*users)
#
# user()


	# Вариант 2

def users(name, surname, year, city, mail, tel):
    print(f"{name} {surname} {year} {city} {mail} {tel}")

users(name=input("Введите имя:"), surname=input("Введите Фамилию:"), year=input("Введите год рождения:")
      , city=input("Введите город проживания:"), mail=input("Введите email:"), tel=input("Введите телефон:"))








		# 3. Реализовать функцию my_func(), которая принимает три позиционных аргумента,
		# и возвращает сумму наибольших двух аргументов.

def func(var_1, var_2, var_3):

    spisok = [var_1, var_2, var_3]
    spisok.remove(min(spisok))
    return  sum(spisok)

try:
    print(f'Результат: {func(int(input("Введите  1-ое число:")), int(input("Введите 2-ое число:")), int(input("Введите 3-ье число:")))}')
except ValueError:
    print("Вы ввели не число!")








		# 4. Программа принимает действительное положительное число x и целое отрицательное число y.
		# Необходимо выполнить возведение числа x в степень y. Задание необходимо реализовать в виде функции my_func(x, y).
		# При решении задания необходимо обойтись без встроенной функции возведения числа в степень.

		# ** Подсказка:** попробуйте решить задачу двумя способами. Первый — возведение в степень с помощью оператора **.
		# Второй — более сложная реализация без оператора **, предусматривающая использование цикла.


		#Вариант 1
# def func(var_1, var_2):
#     return var_1 ** abs(var_2)
# print(f'Результат: {func(abs(float(input("Введите действительное положительное число:"))), (int(input("Введите целое отрицательное число")))) }')

		#Вариант 2


def func(var_1, var_2):

    num = var_1
    var_2 = var_2 - 1
    while var_2 > 0:

        var_2 = var_2 - 1

        num = num * var_1

    return num


print(f'Результат: {func(abs(float(input("Введите действительное положительное число:"))), (abs(int(input("Введите целое отрицательное число"))))) }')








		# 5. Программа запрашивает у пользователя строку чисел, разделенных пробелом.
		# При нажатии Enter должна выводиться сумма чисел.
		# Пользователь может продолжить ввод чисел, разделенных пробелом и снова нажать Enter.
		# Сумма вновь введенных чисел будет добавляться к уже подсчитанной сумме.
		# Но если вместо числа вводится специальный символ, выполнение программы завершается.
		# Если специальный символ введен после нескольких чисел,
		# то вначале нужно добавить сумму этих чисел к полученной ранее сумме и после этого завершить программу.


def values():
    result = 0
    condition = False
    while condition == False :
        num = input("Введите числа через пробел,для выхода нажмите 'q':").split()
        for i in num:

            if i == 'q':
                print(f"Результат: {result}")
                condition = True
                break
            else:
                result += int(i)
        print(f"Сумма чисел: {result}")

values()








		# 6. Реализовать функцию int_func(), принимающую слово из маленьких латинских букв и возвращающую его же,
		# но с прописной первой буквой. Например, print(int_func(‘text’)) -> Text.
		# Продолжить работу над заданием. В программу должна попадать строка из слов, разделенных пробелом.
		# Каждое слово состоит из латинских букв в нижнем регистре. Сделать вывод исходной строки,
		# но каждое слово должно начинаться с заглавной буквы. Необходимо использовать написанную ранее функцию int_func().


def func():
    val = input("Введите слово : ")
    print(val.title())
func()








